# Phase 11 P15 preregistration v2 — final frozen confirmation holdout

## Status

**PREREGISTERED BEFORE ANY P14R OR P15-V2 DATA GENERATION**

The original P15 protocol at seed `759759` was conditional on the exact failed P14 candidate and is retired unexposed with that lineage. This v2 protocol is the final confirmation holdout for the preregistered P14R robust-envelope candidate only.

P15-v2 may run only if the exact frozen P14R candidate passes every required preregistered gate on both P14R seen transfer and P14R protected validation.

## Research question

Does the exact P14R system replicate its availability, uncertainty honesty, bounded primary continuity, and independent rescue behavior on one final untouched set of complete trajectories and compound visual-shift combinations?

P15 is confirmation only. No estimator, calibration rule, radius, grouping, threshold, rescue process, intervention schedule, or scientific constant may change after the P14R candidate freeze.

## Final evidence boundary

- final holdout seed: `869869`
- complete-sequence families: `933..956` (24)
- frames per sequence: `60`
- four truth-independent event strata with six families each: `bootstrap5`, `gap3`, `gap7`, `gap12`

Event definitions are unchanged from P14/P14R:

- `bootstrap5`: primary candidates suppressed at frames `0..4`
- `gap3`: suppressed at `12..14` and `42..44`
- `gap7`: suppressed at `12..18` and `42..48`
- `gap12`: suppressed at `12..23` and `42..53`

The complete sequence remains the split unit. All generated rows are retained.

## Final holdout domains

These ten domains are fixed before any P14R evidence is generated:

1. `edge+dim+temporal_dropout`
2. `small_scale+low_contrast+temporal_dropout`
3. `oblique+blur_noise+temporal_dropout`
4. `dim+low_contrast+temporal_dropout`
5. `edge+oblique+low_contrast+temporal_dropout`
6. `small_scale+oblique+dim+temporal_dropout`
7. `edge+small_scale+oblique+dim+blur_noise+temporal_dropout`
8. `small_scale+oblique+dim+blur_noise+low_contrast+temporal_dropout`
9. `edge+small_scale+oblique+blur_noise+low_contrast+temporal_dropout`
10. `edge+oblique+dim+blur_noise+temporal_dropout`

No P15-v2 row may be generated before P14R protected validation has passed.

## Exact frozen candidate requirement

Before P15-v2 generation, the workflow must verify:

1. the exact P14R candidate JSON SHA-256 emitted before transfer;
2. the exact P14R scientific runner/tests/preregistrations have not changed since candidate freeze;
3. the same candidate digest was used unchanged in seen transfer and protected validation;
4. both result JSON files report every required primary gate passing;
5. seed `869869` has not previously been generated by this authoritative lineage.

Any failed precondition aborts before P15-v2 generation.

## Final holdout power minimums

Minimum available rows per frozen output group:

- base `>=450`
- h3 `>=75`
- h45 `>=60`
- h67 `>=36`
- independent rescue `>=75`

These totals are at least as strict as the aggregate evidence power required by the original severity-cell protocol, while avoiding post-shift empty-cell artifacts. No pooling across output groups is allowed.

## Final primary gates

P15-v2 uses the same numerical standards as P14/P14R with no final-holdout relaxation:

- H1 useful availability: `>=0.92`
- H2 overall 95% coverage: each axis `[0.90,0.98]`
- H3 calibration MACE: `<=0.06`
- H4 interval efficiency: median 95% half-width / overall p95 error `<=1.25`; p95 half-width / p95 error `<=2.25` each axis
- H5 primary-continuity honesty: each axis coverage `[0.88,0.99]`; p95 width/error `<=2.75`
- H6 base-output honesty: each axis coverage `[0.90,0.98]`; p95 width/error `<=2.25`
- H7 inherited severity discrimination: AUROC `>=0.85`, diagnostic only
- H8 high-severity honesty: each axis coverage `[0.88,0.99]`; p95 width/error `<=2.75`
- H9 rescue-output honesty: each axis coverage `[0.90,0.98]`; p95 width/error `<=2.25`
- H10 rescue accuracy: lateral MAE `<=0.15 m`, altitude MAE `<=0.30 m`, lateral p95 `<=0.35 m`, altitude p95 `<=0.70 m`
- H11 rescue effectiveness: at least `85%` recovery of truth-visible primary-unavailable rows

P15-v2 passes only if final group minimums and H1-H6/H8-H11 all pass. H7 remains diagnostic.

## Outcome rule

- If every required P15-v2 gate passes: Phase 11 is complete for this preregistered simulation benchmark. Freeze the result and stop scientific tuning.
- If any required gate fails: freeze the negative/mixed final holdout result. P15-v2 may not be rerun with changed parameters or a replacement seed represented as the same final confirmation.

## Claim boundaries

Even a full pass means only replication within this simulation benchmark.

- `simulation_only = true`
- `safety_acceptance = false`
- `controller_tuning_allowed = false`
- no physical-flight validation claim
- no controller-performance claim
- no claim that a real auxiliary sensor follows the synthetic rescue distribution
- no new raw-camera accuracy claim
