# Phase 11 P15 preregistration — final frozen confirmation holdout

## Status

**PREREGISTERED BEFORE P14 DATA GENERATION AND BEFORE ANY P15 HOLDOUT GENERATION**

P15 is conditional. It may run only if the exact P14 candidate passes every required preregistered gate on both P14 seen transfer and P14 protected validation.

The user explicitly authorized this conditional final-holdout exposure in the conversation on 2026-08-16. That authorization does not permit bypassing any P14 gate.

## Research question

**Does the exact P14 system replicate its availability, uncertainty honesty, and rescue behavior on one final untouched set of complete trajectories and compound visual-shift combinations?**

P15 is confirmation only. No model, estimator, calibration, grouping, threshold, rescue process, intervention schedule, or scientific constant may change after P14 candidate freeze.

## Holdout evidence boundary

- final holdout seed: `759759`
- complete-sequence families: `794..813` (20)
- frames per sequence: `60`
- four fixed event strata with five families each: `bootstrap5`, `gap3`, `gap7`, `gap12`

The same truth-independent event definitions preregistered for P14 are used:

- `bootstrap5`: primary candidates suppressed at frames `0..4`;
- `gap3`: suppressed at `12..14` and `42..44`;
- `gap7`: suppressed at `12..18` and `42..48`;
- `gap12`: suppressed at `12..23` and `42..53`.

P15 generation must not occur before P14 protected validation has passed.

## Final holdout domains

The following ten complete-trajectory compound-shift domains are fixed before any P14 evidence is generated:

1. `edge+oblique+temporal_dropout`
2. `small_scale+blur_noise+low_contrast+temporal_dropout`
3. `dim+blur_noise+low_contrast+temporal_dropout`
4. `edge+small_scale+blur_noise+temporal_dropout`
5. `small_scale+oblique+dim+low_contrast+temporal_dropout`
6. `edge+oblique+dim+low_contrast+temporal_dropout`
7. `edge+small_scale+dim+blur_noise+temporal_dropout`
8. `oblique+dim+blur_noise+low_contrast+temporal_dropout`
9. `edge+small_scale+oblique+blur_noise+low_contrast+temporal_dropout`
10. `edge+small_scale+oblique+dim+blur_noise+low_contrast+temporal_dropout`

The complete sequence is the split unit. All holdout rows are retained.

## Exact frozen candidate requirement

Before P15 runs, its workflow must verify:

1. the exact P14 candidate JSON SHA-256 recorded by the P14 freeze workflow;
2. the exact P14 scientific runner/tests/preregistration have not changed since P14 candidate freeze;
3. the exact P14 candidate was used unchanged in P14 seen transfer and protected validation;
4. both P14 transfer and validation result JSON files record every required gate as passing;
5. P15 seed `759759` has not previously been generated by the authoritative P14/P15 lineage.

Any failure aborts P15 before holdout generation.

## Final holdout primary gates

P15 uses the **same numerical standards** as P14; no holdout-specific relaxation is allowed.

### H1 useful availability
`>=0.92`.

### H2 overall 95% coverage
Each axis in `[0.90,0.98]`.

### H3 calibration curve
MACE `<=0.06` across `{50,68,80,90,95}%` and both axes.

### H4 overall interval efficiency
For each axis:
- median 95% half-width / p95 absolute error `<=1.25`;
- p95 95% half-width / p95 absolute error `<=2.25`.

### H5 primary-continuity honesty
Each axis coverage `[0.88,0.99]`; p95 half-width / p95 error `<=2.75`.

### H6 base-output honesty
Each axis coverage `[0.90,0.98]`; p95 half-width / p95 error `<=2.25`.

### H7 inherited severity discrimination
AUROC `>=0.85`, diagnostic only.

### H8 high-severity honesty
Each axis coverage `[0.88,0.99]`; p95 half-width / p95 error `<=2.75`.

### H9 rescue-output honesty
Each axis coverage `[0.90,0.98]`; p95 half-width / p95 error `<=2.25`.

### H10 rescue accuracy floor
- lateral MAE `<=0.15 m`;
- altitude MAE `<=0.30 m`;
- lateral p95 error `<=0.35 m`;
- altitude p95 error `<=0.70 m`.

### H11 rescue effectiveness
At least `85%` of truth-visible primary-unavailable rows recovered.

The final holdout passes only if required cell minimums and H1–H6/H8–H11 all pass. H7 remains diagnostic.

## Final holdout cell minimums

Per severity cell:

- base `>=150`;
- h3 `>=25`;
- h45 `>=20`;
- h67 `>=12`;
- independent rescue `>=25`.

No pooling is permitted to rescue an underpowered final cell.

## Outcome rule

- **If every required P15 gate passes:** Phase 11 is complete for this preregistered simulation benchmark. Freeze the result and stop scientific tuning.
- **If any required P15 gate fails:** freeze the negative/mixed holdout result. Phase 11 is not declared a complete pass, and P15 may not be rerun with changed parameters or a replacement seed presented as the same holdout.

No post-holdout retuning is authorized by this protocol.

## Claim boundaries

Even a complete P15 pass means only that the frozen Phase 11 method replicated within this simulation benchmark.

- `simulation_only = true`
- `safety_acceptance = false`
- `controller_tuning_allowed = false`
- no physical-flight validation claim
- no controller-performance claim
- no claim that a real auxiliary sensor has the synthetic rescue distribution
- no new raw-camera accuracy claim
