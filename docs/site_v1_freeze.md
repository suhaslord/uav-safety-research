# AegisLand public site v1 freeze

**Status: FROZEN**

The public research-site polishing milestone is closed at repository commit:

`04f8586cff06abfb7f3729c1b1802c8aa77f9f03`

Immutable snapshot branch:

`site-v1-frozen`

The snapshot includes the Phase 1–10R archive, phase-specific hero animations, the 12-stage research progression, first-party Aegis favicon work present at that repository boundary, simulation-only research labeling, and the Phase 10R latest-published-frontier presentation.

## Freeze rule

Phase 11 research development must not require edits to the frozen public-site snapshot. New research work occurs on `phase11-domain-shift-development`. Website changes after this point should be treated as a separate post-v1 presentation milestone rather than silently modifying the frozen site record.

## Research boundary

The site freeze is a presentation/reproducibility boundary only. It does not change any scientific evidence, Phase 10R frozen results, or safety claims.

- `simulation_only = true`
- `safety_acceptance = false`
- `controller_tuning_allowed = false`
