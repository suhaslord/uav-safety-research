# Phase 10R validation reproducibility pointer

Run the preregistered development + trajectory-held-out validation through `.github/workflows/phase10r-development-validation.yml`. The authoritative completed evidence is GitHub Actions run `31919732007`, artifact `9256050160`, candidate freeze `e1d566f8baa47bf10f9bdf39dd5988724208be80`.

Do not reinterpret a rerun as a new hidden validation result: validation seed `271828` is now seen evidence.
