name: Historical Phase 6B Frozen Evaluation (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled historical workflow
        run: |
          echo 'This workflow is intentionally disabled as an experiment runner.'
          echo 'Phase 6B held-out seeds 868686 and 878787 were already executed in historical run 31355377934.'
          echo 'They are permanently seen and must not be reused as held-out validation for later revisions.'
          echo 'See docs/research_integrity_recovery.md on the recovery branch for the replacement protocol.'
