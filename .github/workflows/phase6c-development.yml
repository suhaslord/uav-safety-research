name: Phase 6C Development Simulation

on:
  push:
    paths:
      - '.github/workflows/phase6c-development.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  development-simulation:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Full test suite
        run: pytest -q
      - name: Run paired synthetic Phase 6C development matrix
        run: |
          python scripts/run_phase6c_development.py \
            --episodes 30 \
            --seed 626262 \
            --calibration-seed 616161 \
            --out results/phase6c_development
      - name: Decompose simulated touchdown failures
        run: python scripts/analyze_phase6_failures.py --results results/phase6c_development
      - name: Upload Phase 6C development artifact
        uses: actions/upload-artifact@v4
        with:
          name: phase6c-development-simulation
          path: results/phase6c_development
          if-no-files-found: error
          retention-days: 45
