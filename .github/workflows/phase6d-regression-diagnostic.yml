name: Phase 6D Regression Diagnostic

on:
  push:
    paths:
      - '.github/workflows/phase6d-regression-diagnostic.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  regression-diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Full test suite
        run: pytest -q
      - name: Replay already-seen Phase 6D development cases
        run: |
          python scripts/diagnose_phase6d_regressions.py \
            --calibration-seed 616161 \
            --temporal-calibration-samples 180 \
            --component-calibration-samples 280 \
            --out results/phase6d_regression_diagnostic
      - name: Upload Phase 6D diagnostic
        uses: actions/upload-artifact@v4
        with:
          name: phase6d-regression-diagnostic
          path: results/phase6d_regression_diagnostic
          if-no-files-found: error
          retention-days: 45
