name: Phase 11 P8 candidate freeze — retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-stop:
    runs-on: ubuntu-latest
    steps:
      - name: P8 is permanently stopped before protected validation
        run: |
          echo "P8 development completed in workflow run 31973942401 and failed preregistered efficiency gates."
          echo "Seeds 352352, 363363, 374374, and 385385 are permanently seen."
          echo "Protected validation seed 396396 was not exposed."
          echo "Do not rerun P8 as unseen evidence."
